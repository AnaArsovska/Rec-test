# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Rec-test' do

	pod 'Firebase/Core'
	pod 'Firebase/Auth'

  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Rec-test

  target 'Rec-testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Rec-testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
